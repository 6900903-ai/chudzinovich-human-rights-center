name: CHUDO Viasna Export Discovery

on:
  pull_request:
    paths:
      - '.github/workflows/viasna-discovery.yml'
      - 'scripts/discover-viasna-export.mjs'
      - 'scripts/lib/viasna-export-discovery.mjs'
      - 'scripts/lib/viasna-direct-export.mjs'
      - 'scripts/adapters/viasna.mjs'
      - 'tests/viasna-export-discovery.mjs'
  push:
    branches: [main]
    paths:
      - '.github/workflows/viasna-discovery.yml'
      - 'scripts/discover-viasna-export.mjs'
      - 'scripts/lib/viasna-export-discovery.mjs'
      - 'scripts/lib/viasna-direct-export.mjs'
      - 'scripts/adapters/viasna.mjs'
      - 'tests/viasna-export-discovery.mjs'
  schedule:
    - cron: '23 */6 * * *'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: viasna-export-discovery-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unit:
    if: github.repository == '6900903-ai/chudzinovich-human-rights-center'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: '22'
      - name: Test fail-closed export discovery
        run: node tests/viasna-export-discovery.mjs

  live-discovery:
    if: github.event_name != 'pull_request' && github.repository == '6900903-ai/chudzinovich-human-rights-center'
    needs: unit
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: '22'
      - name: Probe official Viasna CSV export
        env:
          VIASNA_DATA_REUSE_GATE: PASS
          FETCHER_SECURITY_GATE: PASS
          VIASNA_SOURCE_PAGE_URL: https://prisoners.spring96.org/ru/list
          VIASNA_SOURCE_CSV_URL: https://prisoners.spring96.org/ru/list?format=csv
          VIASNA_SOURCE_LOCALE: ru
          VIASNA_MIN_OBSERVATIONS: '5000'
        shell: bash
        run: |
          set +e
          node scripts/discover-viasna-export.mjs 2>&1 | tee /tmp/viasna-export-discovery.txt
          status=${PIPESTATUS[0]}
          set -e
          {
            echo '## Viasna export discovery'
            echo '```text'
            cat /tmp/viasna-export-discovery.txt
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
          if [ "$status" -ne 0 ]; then
            if grep -q 'VIASNA_DISCOVERY_HTTP_403' /tmp/viasna-export-discovery.txt; then
              echo 'VIASNA_EXPORT_DISCOVERY=BLOCKED_HTTP_403 manual_or_authorized_export_required=true public_database_mutated=false' | tee -a "$GITHUB_STEP_SUMMARY"
              exit 0
            fi
            exit "$status"
          fi
